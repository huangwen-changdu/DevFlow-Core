# Skill Sync After Update

- Trigger: skills updated, commands updated, runtime scripts updated, SKILL.md changed, update complete, sync latest skills, user-level skills directories, .claude skills, .codebuddy skills, .codex skills, .workbuddy skills.
- Lesson: After updating a reusable runtime surface, skills alone are not enough. User-level sync also needs `commands/` and the runtime checker scripts matching `scripts/devflow-*.js`.
- Next action: Next time completing a skills, commands, or runtime-script update, overlay-sync `skills/`, `commands/`, and `scripts/devflow-*.js` to `C:\Users\huangwen\.claude`, `C:\Users\huangwen\.codebuddy`, `C:\Users\huangwen\.codex`, `C:\Users\huangwen\.workbuddy`, `C:\Users\huangwen\.zcode`, and `C:\Users\huangwen\.dsh`; do not delete target-only files or copy maintainer-only scripts. Sync rules only to targets with an established `rules/` entry point. There are **four** distribution copies to keep in step, not two: run `node dsh/plugins/dsh-devflow/scripts/sync-assets.js` for the packaged DSH assets (parity asserted by validate-skill-triggers.js), and separately **mirror `plugins/devflow/` by hand** — the Codex plugin has no sync script at all, so `verify-plugin.js` only reports drift, it never repairs it. Mirror by walking the intersection: every directory already present under `plugins/devflow/skills` copied from `skills/<same name>`, plus every `scripts/*.js` that also exists under `plugins/devflow/scripts`. When the skill behavior changed, also add the matching evidence line to the flow-self-test scenario and, when the scenario has a counterpart, `capability-eval-scenarios.json` scenarioEvidence.
- Scope: project
- Related: `skills/`, `commands/`, `scripts/devflow-*.js`, `scripts/install-devflow-user.js`, `npm run install:user -- --home <target>`, `plugins/devflow/scripts/verify-plugin.js`
- Evidence: user installer entry list and user-install verification command. 2026-09-11：六个 home（.codex/.claude/.codebuddy/.workbuddy/.zcode/.dsh）先 --check 报 5 家 8 changed（含 1e30afd 未同步的 4 个 preset 文件）、.dsh 4 changed（本次 4 个运行时文件）；--write --force 每家 54 文件覆盖；复审 6/6 Check passed exit 0。安装器 overlay 不 prune，目标独有文件保留。2026-09-16：六个 home 一律先报 11 changed（.dsh 另有 2 missing），`--write --force` 后 6/6 Check passed；`node scripts/validate-user-installer.js` 退出码 0；Codex 插件副本手工镜像 10 个文件后 `verify-plugin.js` 通过（14 skills / 5 self-tests）。
- Invalidation: Revise when user-level runtime scope or installer behavior changes.
