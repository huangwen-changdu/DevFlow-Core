# Skip Path Needs Receiver And Success Edge

- Trigger: skip Brainstorm, bypass gate, new Core direct path, Design-lite without Brainstorm, add route exception
- Lesson: A Core skip permission is illegal until the receiver entry, named success edge, leftover opposite command sentence, and scenario evidence all name the same successor. Phrase-presence validators can PASS while Cut still requires an approved Spec and `/devflow` still says Design-lite waits for Brainstorm.
- Next action: Next time adding a bypass, first write the producer artifact, receiver entry, and success-edge name, then delete leftover opposite sentences and add a contradiction assertion. Do not only add "may skip" language.
- Scope: project
- Related: skills/devflow-core/SKILL.md, skills/devflow-cut/SKILL.md, commands/devflow.toml, scripts/validate-devflow.js, scripts/validate-route-consistency.js
- Evidence: v52 find-fault found skip-Brainstorm with no Cut predecessor; after Depth C + Design-lite contract + Fast exclusion, `npm test`, `host:verify`, `trigger:verify`, `route:verify`, and `capability:eval` passed. Capability-eval also failed on `selects` vs `select` exact scenario phrasing.
- Invalidation: if skip-Brainstorm is removed, or skip successor is no longer Design-lite Depth C
