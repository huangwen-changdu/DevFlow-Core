# 2026-09-16

## 计划契约 v1→v2 落地缺口诊断（只读评审，未改代码）

用户反馈「计划文档落地的内容差点意思，没有最初的好」。诊断结论：

**契约层已切 v2，下游消费面未同步**（机检全绿也抓不到，因为现有断言只验新契约"存在"，不验旧字段"消失"）：

- `commands/devflow-plan.toml:15-53` 整份仍是 v1 模板（Architecture / Tech Stack / Spec coverage / Task type / Interfaces / Current·Target behavior / Change mechanics / Call impact / Steps / Comments）。09-10 计划的 File Structure 只列了 `commands/devflow.toml`，漏了 `commands/devflow-plan.toml`。
- `skills/devflow-build/SKILL.md:95` fan-out 并行判据仍用 `Interfaces` / `Produces`；`:131` 注释纪律仍读 plan 的 `Comments` 字段。
- `skills/devflow-prove/SKILL.md:27`、`:29`、`:46`、`:63` 仍要求读 `Prewalk Execution Trace` / `Current Handoff Facts` / `File Structure`（仅 `:104` 有 legacy 兜底措辞）。
- `skills/devflow-prove/references/code-review-checklist.md:7`、`proof-recovery-methods.md:26`、`flow-self-test.md:419/691`、`skills/devflow-core/references/reference-projects.md:70`、`scripts/capability-eval-scenarios.json:179` 同类残留。
- 三份副本（根 skills / `plugins/devflow/skills` / `dsh/.../assets/skills`）同步带着这些残留。

**结构层（有意取舍，非缺陷）**：v2 删除 Prewalk 三件套、Interfaces、Current/Target behavior、Change mechanics、Call impact、File Structure、Global Constraints、Steps、Comments、Task type；计划体量 277-396 行 → 44-75 行。补洞机制是 `Change` 的「机制不可推时附最小可执行改法」，属自由裁量、checker 不验 —— 交接密度从结构保证降级为作者自觉。

**证据**：`node scripts/validate-devflow.js` PASS、`node scripts/devflow-plan.js --index` PASS、`devflow-budget` 160464/280000 bytes、15 个 SKILL.md 主体最大 15341 bytes。四份 v2 计划均 ≤7 任务，大计划场景未检验。

**交付物**：`docs/2026-09-16-plan-contract-v2-drift-review.md`（用户选定"先只要一份诊断报告"，未改任何技能/脚本/副本）。报告含 9 条漂移清单（P0-1 命令模板、P0-2 spec-plan-methods.md:13 为最高优先）、非缺陷白名单（避免误改 legacy 分支与合法的 legacy 提及）、4 批次修复顺序与事实边界。补记 P1-8 = `README.md:256` 的 Superpowers 能力行仍宣称 `Spec coverage` tracing（v2 已删）。

## 外部对照：Superpowers 6.3.0（`D:\Project\Github\superpowers`）

- 与 DevFlow 计划 v2 走相反路线：真实计划 1096 行、步骤内嵌完整代码、执行者"Follow each step exactly"、保留 `Interfaces: Consumes/Produces` 与 `## Global Constraints`、`Files` 带行号范围（`Modify: path:123-145`）。
- **关键机制**：交接走**文件**——`scripts/task-brief PLAN_FILE N` 抽 `task-N-brief.md`，执行者回写 `task-N-report.md`；理由写明"贴进 dispatch 的内容会长期驻留上下文并被反复重读，hand artifacts over as files"。这正是 DevFlow v2 缺的"侦查证据落点"，且不要求计划变胖。
- 其他可吸收点：执行者回报状态四分（DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED），DevFlow 只有 BUILD_BLOCKED；明确"Never dispatch multiple implementation subagents in parallel (conflicts)"，而 DevFlow 提供 fan-out 且其并行判据正依赖已删的 `Interfaces`。
- 不吸收：计划内嵌代码（双份维护）；每任务必派 reviewer 子代理（DevFlow 单次 Prove 更省）。
- 已写入诊断报告第 7 节；报告增至 120 行，`validate-devflow.js` 与 `--index` 仍全绿。

## 计划密度回归诉求（本轮路由）

用户诉求：不要为了瘦身而瘦身，v1 与 Superpowers 两种形态都可以，当前 v2 太简陋。

- 路由：Core -> `devflow-brainstorm`（Design 路由，契约变更=protected risk，depth hint `deep`）。
- **必须遵守的两张卡**：
  - `subjective-quality-structure-vs-behavior.md`：主观质量诉求先分清结构问题 vs 行为/能力问题；对 LLM prompt **可选格式=行为删除**，正解是强制结构+自然措辞；验收要测默认行为而非机制存在。
  - `plan-density-executor-contract.md`：密度跟随执行者契约；本仓执行者=不同会话/模型、可加载技能、可读仓库锚点，故 v2 六字段为基线；superpowers 全量内嵌代码只在"无技能无仓库"前提下必要。失效条件=执行者前提改变或 v2 六字段被替换。
- 我的初步拆解（待用户确认）：(a) 真实载体缺口——锚点侦查证据移出计划后无落点（对话消失、卡片不收），属行为问题，修法是给载体不是加字段；(b) 两个字段消费者仍在——`Interfaces`（fan-out/单子代理下执行者只见自己任务）、`Global Constraints`（Spec 逐字项目级不变量）；(c) v1 的一部分是 checker 礼仪而非执行者能力，整体回退会请回已证伪密度；(d) 用户读到的可能是**不一致的 v2**（v1 模板与 v2 指令同时生效），应先修漂移再评估。
- 用户已确认回声。追加诉求：区分"能从开发流程与找事实依据中顺下来"的点 vs "会让 AI 伪造事实依据"的点。
- **用户确认的门槛判据**：恢复项采用**机械触发 + 机检强制**（判断式"按需"在 LLM prompt 里等于删除）。
- **实测硬约束（设计前必读）**：`skills/devflow-prove/SKILL.md` = 15341/15360 字节，**只剩 19 字节**，任何给它加字的方案必须先删等量内容；`AGENTS.md` = 6960/8192，余 1232 字节；skills 总量 160464/280000。
- 已按 Brainstorm 协议产出 Confirmed request 并在 `docs/requirements.md` 建 `open` 行（2026-09-16，深度待选）；`node scripts/devflow-plan.js --index` 仍 PASS。
- 用户选 **Depth A**；台账该行已填 深度=A、落地物=spec 路径。
- **交付物**：`docs/specs/2026-09-16-plan-contract-evidence-density.md`（设计契约，R1-R9 / Non-goals / 四选项对比 / D1-D7 / A1-A7 / Q1-Q3）。`node scripts/devflow-spec.js` 判定 PASS（Missing none / Unresolved none / Vague none），`validate-devflow.js` PASS，`--index` PASS。当前处于 Spec 审批门，等待用户批准后直接进 Cut。
- 设计要点：R1 证据两级准入（L1 命令+输出 / L2 机械复制）；R2 头部全局不变量逐行复制自 Spec Non-goals 或 Cut Decision；R3 命令式侦查载体（取代 v1 自述式 Prewalk）；R4 跨任务通道仅在 single-subagent/fan-out 时必填；R5 机械触发+机检强制（判断式"按需"视为删除）；R6 机检须含反向用例；R9 prove 主体须以替换而非追加方式落地（19 字节余量）。
- 注意 Spec checker 的 `unresolvedPatterns` 含 `<[^>\n]+>`，**spec 正文禁止出现任何尖括号占位符**，路径必须写实。

## Cut 与 Plan（同一需求）

- Cut 强度 `full`，结果 **CUT_PASS**（Depth A → 直接进 `devflow-plan`）。真实减法 6 项，最重要的一刀：**不新增 `Global Constraints` 独立小节，改用既有头部 `Not doing` 字段承载项目级不变量**（4 份 v2 计划已在如此使用）；其余否决项：文件形态交接载体、每任务强制 `Interfaces`、checker 重跑命令、跨任务通道并入 `Change` 散文、新增独立校验脚本。
- Cut 顺带裁定了 Spec 的三个 Open Questions：**Q1** 侦查命令只校验形态 + Prove 抽查（checker 保持纯静态、零副作用，便于拷贝到 target 项目）；**Q2** `Not doing` 的来源在 Spec Non-goals 与 Cut 行冲突时**以 Cut 行为准**；**Q3** 旧字段断言覆盖 skills/commands/AGENTS.md/README.md 四面，`docs/features/devflow-core.md` 只改能力说明段、日期化版本历史表不动，`Interfaces` **不列入**永久移除清单（它以条件形态回归）。
- 自举约束（沿用 09-10 先例 R10）：本计划自身按现行 v2 契约产出并通过现行 checker；新契约从下一个计划起生效。写进了计划头部。
- **交付物**：`docs/plans/2026-09-16-plan-contract-evidence-density.md`（5 个任务，全 v2 六字段）。`node scripts/devflow-plan.js <plan>` PASS（5/5 task ok）、`--index` PASS、`validate-devflow.js` PASS。已加 `docs/plans/INDEX.md` 行（Status draft）。
- **plan checker 的两条硬规则（踩过）**：① `Modify:` 行第二段必须有可识别锚点——`` symbol: `标识符` ``、`heading ## 标题`、或 `function|class|method|const|heading|section|key <名>`，**行号不被接受**；② `Change` 必须含 `implementationVerbPattern` 里的动词（`parse|validate|require|reject|map|filter|return|add|remove|replace|insert|delete|set|check|compare|append|emit` 或 `增加|添加|解析|验证|拒绝|替换|输出|收集|要求|检查`），"运行/写入/新增/改用"都不算。
- Spec checker 的 `unresolvedPatterns` 含 `<[^>\n]+>`：**spec 正文禁止任何尖括号占位符**。

## 本次落地完成（Depth A 全链路）

- **终态**：`npm run verify:all` 退出码 0（18 项）；`devflow-plan.js --self-test` 退出码 0（含 7 例新反向用例）；`--index` PASS；32 份计划改动前后退出码差异 0；budget 162919/280000 且 `devflow-prove/SKILL.md` 由 15341 降到 15337（余 23）；sync 幂等 + DSH 子包自测 5 场景 + Codex 插件 14 skills/5 self-tests 通过；版本 0.6.0 / 0.10.0 / 0.4.0（`npm publish` 待用户执行）。
- 计划 `Status: done` + `Landed` 已写；计划索引行 `done`；需求台账行 `landed`；新学习卡 `evidence-form-determines-falsifiability.md` 已入索引；诊断报告加了 Fate 行指向本次落地。
- **Build 期发现的两个真缺陷**：① `Interfaces` 未参与 v2 字段边界判定 → `Files` 块吞掉接口行（真实计划同样会踩），已加 `v2ConditionalFields`；② 新断言抓出原清单外的两处同族残留（`commands/devflow-spec.toml` 的 v1 计划头部、`flow-self-test.md:699` 缺 legacy 说明）。
- **已知残留（未做，需用户决定）**：`docs/features/devflow-core.md` 的日期化版本历史表按计划 `Not doing` 未加 v55 行，能力台账缺本轮记录；禁用字段清单未含 `Comments`/`Steps`/`Architecture`/`Tech Stack`/`External Skills`。

## 本地用户级同步（同日）

- 六个 home（`.codex` / `.claude` / `.codebuddy` / `.workbuddy` / `.zcode` / `.dsh`）`--check` 一律报 **11 changed**（`.dsh` 另报 2 missing），`--write --force` 后 **6/6 Check passed**；`node scripts/validate-user-installer.js` 退出码 0；`npm run verify:all` 退出码 0。
- **新踩到的坑（已写进 `skill-sync-after-update.md`）**：分发副本有**四份**不是两份——`dsh/plugins/dsh-devflow/assets` 有 `sync-assets.js`，但 **`plugins/devflow/`（Codex 插件）没有同步脚本**，`verify-plugin.js` 只报漂移不修复，必须按交集手工镜像（`plugins/devflow/skills` 下已有目录 ← `skills/<同名>`，外加两边都存在的 `scripts/*.js`）。
- 顺带发现一处**尚未补的覆盖缺口**（未改，等用户定）：`skills/devflow-prove/references/flow-self-test.md:416` 的 Scenario 5A 仍把计划契约描述为"瘦头部 + 六字段 + Cut/Rejected + Progress"，**未包含 `## Recon` 与证据两级准入**；`capability-eval-scenarios.json` 里没有计划场景，所以只涉及 flow-self-test 一处。同步卡要求"skill 行为变更后补对应场景证据行"，本次未做（超出已批准计划的触达表）。
- **拟定的计划字段证据阶梯（关键设计规则，待确认）**：L1 可重跑命令+当时输出（难伪造，重跑即验伪）> L2 从既有产物机械复制（可机检 diff/字符串出现）> L3 指向必须存在的路径或符号（只证存在不证读过）> L4 自由散文自述（无保护）。**v1 的 Prewalk 三件套全是 L4，且 checker 只校验形状（5 行型/8 facts/≤12 项），形状校验本身即伪造诱因**；v2 保留的两个成功项（`## Progress` 证据、`Landed`）恰好都是 L1。结论：能顺下来的=Source/Global Constraints/Cut 行（L2）+ Verify/Progress 证据/Landed（L1）+ Recon 载体（但形式必须改为命令+输出）；不恢复=Prewalk 三件套、Current/Target behavior、Call impact、强制 Change mechanics 伪代码、Task type（皆 L4 或纯声明）。`Interfaces` 是唯一真正不可事前推导的候选（断言尚不存在的符号），按该判据应降级为"事后核对"或放弃。
